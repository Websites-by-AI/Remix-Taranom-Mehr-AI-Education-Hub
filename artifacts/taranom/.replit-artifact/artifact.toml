kind = "web"
previewPath = "/"
title = "ترنم مهر — پلتفرم هوشمند کنکور"
version = "1.0.0"
id = "artifacts/taranom"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18881

[services.development]
run = "pnpm --filter @workspace/taranom run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/taranom", "run", "build" ]
publicDir = "artifacts/taranom/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18881"
BASE_PATH = "/"
